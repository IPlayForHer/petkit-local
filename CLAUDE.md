@AGENTS.md

<!--
Claude Code reads CLAUDE.md, not AGENTS.md
(https://code.claude.com/docs/en/memory#agents-md), so this file exists to pull
the shared instructions in. An import rather than a symlink: creating one on
Windows needs Administrator rights or Developer Mode, and a clone with
core.symlinks=false checks it out as a text file instead of a link.

Anything that applies to every agent goes in AGENTS.md. Put something here only
if it is genuinely Claude-Code-specific.
-->
